name: Temp Format Projects

on:
  push:
    branches:
      - feature/projects-production-readiness
    paths:
      - .github/workflows/temp-format-projects.yml
      - src/features/projects/**

permissions:
  contents: write

jobs:
  format:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout branch
        uses: actions/checkout@v7
        with:
          ref: feature/projects-production-readiness
      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm
      - name: Install dependencies
        run: npm ci
      - name: Format Projects files
        run: npx prettier --write "src/features/projects/**/*.{ts,tsx,css}"
      - name: Commit exact Prettier output
        shell: bash
        run: |
          if git diff --quiet; then
            echo "No formatting changes needed."
            exit 0
          fi
          git config user.name "Legacy CI"
          git config user.email "ci@legacycore.app"
          git add src/features/projects
          git commit -m "style(projects): apply exact prettier output"
          git push origin HEAD:feature/projects-production-readiness
